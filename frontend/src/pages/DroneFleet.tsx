import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const DroneFleet: React.FC = () => {
  return (
    <div>
      <Title level={2}>Drone Fleet</Title>
      <Card>
        <p>Drone fleet management interface coming soon...</p>
      </Card>
    </div>
  );
};

export default DroneFleet;